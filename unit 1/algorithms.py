# Create a function that will return the largest 
# and the smallest an array 
numbers ={0,1,5,18,20,39,48,83,89,72,90,18282,1292}
# min / max

def minMax():
    x = min(numbers)
    y = max(numbers)

    print(x ,y) 

minMax()

# Create a functon that will reverse the characters in a string.
# your string must be passed into the function as a parameter 
# EXAMPLE: book = koob
# car = rac 

def reverseString(word)
    text = word(::=1)
    print(text)

reverseString('anything')    