# Activity:
# Create a function that will loop through 
# and print eatch number in the list and stops at the 48
# in your function, the list and the value
# that needs to be found should be passed as arguments.

numbersList = [1,20,39,48,72,83]

def numbering(numberList, vslue)

int *students;
int numStudents = 12;
students = calloc(numStudents, sizeof(*students));
printf("%d", numStudents * sizeof(*students)); // 48 bytes




<ol start="50">
  <li>Coffee</li>
  <li>Tea</li>
  <li>Milk</li>
+</ol>

numberList()